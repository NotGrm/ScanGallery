- if notice	
	%div.alert.alert-success
		%button.close{"data-dismiss"=>"alert"} ×
		%p#notice= notice

= render 'nav'

%div
	= image_tag @page.image.url

= render 'nav'

.well
	= link_to 'Add Next Page', new_manga_chapter_page_path(@manga, @chapter), :class => "btn btn-primary"
	
